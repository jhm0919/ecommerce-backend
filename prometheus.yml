global:
  scrape_interval: 15s  # 15초마다 수집

scrape_configs:
  - job_name: 'ecommerce-customer'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['app:8080']  # ★ 컨테이너 이름:포트